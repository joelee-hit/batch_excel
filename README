# 批量处理excel的工具

基于 PyQt 开发桌面应用

删除远程仓库中所有历史提交并只保留当前状态：

```bash
git checkout --orphan temp_branch  # 创建新分支
git add -A
git commit -m "first commit"
git branch -D main  # 删除原来分支
git branch -m main  # 将当前分支改名为 main
git push -f origin main  # 强制推送
```